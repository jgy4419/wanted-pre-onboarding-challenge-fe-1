import React from 'react';
import AuthChoice from '../component/auth/AuthChoice';

const Auth = () => {
    return (
        <div>
            <AuthChoice/>
        </div>
    );
};

export default Auth;